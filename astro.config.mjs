import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://SatishKKumar.github.io', // GitHub Pages root
  output: 'static',                       // required for GitHub Pages
});